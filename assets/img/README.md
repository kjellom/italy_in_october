Drop your own photos here, named to match each destination's id, e.g.:

positano.jpg
capri.jpg
rome.jpg

The site will pick them up automatically. See the `id` field for each
destination in script.js for the exact filename to use. If a file is
missing, the card just shows its accent color instead — nothing breaks.
